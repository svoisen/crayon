src.loc=${basedir}/src
test.loc=${basedir}/test
lib.loc=${basedir}/lib
embed.loc=${basedir}/embed

bin.dir=bin
bin.loc=${basedir}/${bin.dir}
release.loc=${basedir}/release

report.loc=${basedir}/report
build.loc=${basedir}/build

app.class=CrayonIDE.as
app.descriptor=CrayonIDE-app.xml
test.runner=CrayonRunner.as

locale=en_US
cert.storetype=pkcs12
cert.loc=${build.loc}/cert.p12
cert.name=Test Certificate
cert.org.unit=
cert.org.name=Voisen
cert.country=US
cert.keytype=2048-RSA
cert.pass=smoothcrank