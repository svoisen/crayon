print "Starting"

set x to 10 
set y to 10 

repeat 10 times
  draw "circle" with 
    radius as 10
    color as random with min as 0, max as 0xFFFFFF
    center as (x, y)
  end
  set x to x + 20
end
